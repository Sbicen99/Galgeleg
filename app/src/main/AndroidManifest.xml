<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.galgeleg">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/Galgespillet"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:label">
        <activity android:name=".Test_klasse"></activity>
        <activity android:name=".Highscore_spillet" />
        <activity android:name=".HentOrdRegneark" />
        <activity android:name=".Next_level" />
        <activity android:name=".Tabt_galgespillet" />
        <activity android:name=".Vundet_Galgespillet" />
        <activity android:name=".Hjaelp" />
        <activity android:name=".Galge_spillet" />
        <activity android:name=".Galge_spillogik" />
        <activity android:name=".Hovedmenu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>